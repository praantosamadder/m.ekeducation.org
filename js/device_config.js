// IF display width is less than display height

// IF device is not mobile, then redirect to ekeducation.org

// IF current browser is Chrome on Android, then change stylesheets to Android compatible version
